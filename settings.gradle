rootProject.name = 'SCCSM'
include 'common'
include '1.10.2', '1.11.2', '1.12'
rootProject.children.each { project ->
	project.buildFileName = '../common.gradle'
	project.name = "${rootProject.name}-${project.name}"
}